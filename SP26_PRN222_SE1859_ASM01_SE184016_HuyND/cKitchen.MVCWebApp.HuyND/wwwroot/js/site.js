.inventory - scroll {
    max - height: 500px; /* Giới hạn chiều cao (tùy chỉnh theo số dòng) */
    overflow - y: auto;   /* Bật thanh cuộn dọc khi quá chiều cao */
    scroll - behavior: smooth; /* Cuộn mượt */
}
